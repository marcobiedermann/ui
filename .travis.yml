language: node_js
node_js:
  - stable
cache:
  directories:
    - node_modules
before_install:
  - npm install -g @storybook/cli
script:
  - npm test
  - npm run build
deploy:
  provider: releases
  file_glob: true
  file: /*
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  on:
    tags: false
